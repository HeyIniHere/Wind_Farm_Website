/**
 * Hero background for the Explore page. Replace `explore-hero-bg.png` in
 * `src/assets/images/` with your own image (keep the same filename), or change
 * the import below to point at another file.
 */
import exploreHeroBg from "assets/images/Explore.png";

export default exploreHeroBg;
